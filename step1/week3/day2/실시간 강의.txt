LangChain 구조화 출력 방법
1. data class (최근)
2. Pydantic

arg_schema=SaveStructureRequestInput <- input
? 왜 이렇게 하는가
- 상용프로그램을 Agent화 할 때, LLM이 함수 호출에 어떤 데이터가 들어가야하는 지에 대한게 없으면 굉장히 혼란스러움
- 그렇다고 시스템 프롬프트에 전부 넣자니 컨텍스트가 낭비됨
- 입력 전에 한 번 검증 받고 입력?

Agent trace 기본적으로 체크
- result.tool_call
	- arguments가 어떻게 들어갔는지 "반드시" 확인
	- "의도대로" 작동하는지 반드시 확인
- 심화: sub agent의 tool_call <- trace에 안 나올 수도 있음
	-> LangSmith 등 사용
- 로그를 계속 추적하면서 의도대로 작동하는지 검증하는 것이 가장 중요


#Agent 평가
1. 더미 / 프롬프트 검사
2. QnA 데이터 페어
 생성 모델 검증 3가지 방식
	1) 사람이 직접 봄 ( Golden Standard )
	2) 전통적인 딥러닝 -> 생성값과 정답을 벡터로 만들어 비교
	3) LLM을 사용


3주차 목표
- class들이 tool을 실행할 수 있도록 형식을 잡아주고
- 입력 검증, 앱 DB에 등록

1. 구조화 / 저장
2. 삭제 ( 조회 -> 삭제 흐름, tool 조합 흐름 고려 )
- System Prompt에 few-shot, CoT 등 적극 사용

------------------------------------------------------------------------------------------------------------------------------------

LLM도 결국 Transformer 모델의 특성을 따라감
- Attention의 특성: 중요하지 않은 정보가 빠르게 소실됨

CNN <-> Transformer 유사점
- 앞에서 Attention, CNN으로 특징 추출 -> 뒷쪽에서 MLP를 사용하여 학습, 패턴 학습

MLP 이전에 데이터 차원 확장 -> 축소
- 패턴들을 늘린 후 압축하며 지식?들이 저장된다? 대화들의 패턴이 저장된다?

MoE: LLM의 구조, MLP가 여러개 있음

temperature: logit 값을 나눠주는 값 -> 확률값들이 바뀜!
- 0이면 가장 확률이 높은 예측값만 사용
- 안정적, but 다양한 답변을 받을 수 없음

LLM이 token을 선정할 떄 top K 처럼 상위 몇 개만 쓰기도 함

Transformer: Attention 구조를 사용해 특징을 추출하고, MLP를 사용하여 패턴을 추출하여 문장을 만들어 낸다고 이해

------------------------------------------------------------------------------------------------------------------------------------

하네스 엔지니어링?? 이게 무엇인가
- 정립된 용어라기보단 막 얘기하다가 지칭한게 쓰이고 있는 느낌
- Agent를 제어하는 방식이 계속 바뀜
	Agent -> Multi-Agent -> Sub-Agent ...
- Agent가 정립이 안됐을 때 LangGraph 출현 -> Multi-Agent 표방
	문제: C언어처럼 Multi-Agent를 구현해야 함 ( 어떤 tool을 쓰고 분기로 가고 Context로 갈지 하나 하나 다 관리해줘야 함. 자동화 느낌이 아님... )
- 이 때 Claude Code가 tool call로 Sub-Agent 시스템 정립
	문제: 작업이 커지면 한번의 Context로 종료가 안 됨
	Context를 넘길 때 정리된 md 파일로 남기기 - Sub-Agent가 활용

Harness
- Agent를 제어하기 위한 방법론
- 결국엔 Prompt로 제어가 됨 -> Prompt를 어떻게 구성하느냐에 따라 전체적인 동작이 제어가 됨
- 모델이 주어진 기능을 제약사항 내에서 기능하도록? 하는것
- LLM Model 선정하는 것도 Harness로 볼 수도 있긴함

구린 LLM 모델 일부러 써보면 공부에 도움 많이 될 것
- 프롬프트를 더 잘 써줘야 함

LLM 프롬프트 영어로 쓰면 성능 꽤 유의미하게 올라감
- 근데 요즘엔 모델들이 좋아져서 그렇게 까지 큰 차이는 없는듯?


최신 정보?
- Claude Schedule 써서 최신학회 정보 정리해달라고 ㄱㄱ
